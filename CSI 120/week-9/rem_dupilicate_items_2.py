# remove all occurrences of the specified item
import os
os. system ("cls")
counter = 0
list_length = int(input("How many times are in your list"))

user_list = [] # initialize an empty list to store the items

